export type Claim = { id: string; projectId: string | null; taskId: string | null; subject: string; predicate: string; value: string; classification: string; status: string; confidenceBasisPoints: number; validFromAt: string | null; validUntilAt: string | null; validFromCommit: string | null; validUntilCommit: string | null; sourceEvidenceArtifactId: string | null; sourceKnowledgeArtifactId: string | null; supersededByClaimId: string | null; resolutionNote: string; recordedAt: string; updatedAt: string };
export type Decision = { id: string; projectId: string; taskId: string | null; question: string; selectedOption: string; rejectedAlternatives: string[]; rationale: string; consequences: string[]; status: string; effectiveRepository: string | null; effectiveFromCommit: string | null; effectiveUntilCommit: string | null; evidenceArtifactIds: string[]; supersededByDecisionId: string | null; resolutionNote: string; createdAt: string; updatedAt: string };
export type EvidenceRow = { artifact: { id: string; title: string; type: string } };
