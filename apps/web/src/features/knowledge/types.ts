export const artifactTypes = ['legacy_document', 'git_reference', 'research', 'specification', 'decision', 'evaluation', 'handoff', 'project_state', 'roadmap', 'agent_instructions'];
export type Milestone = { id: string; projectId: string; flowId: string | null; name: string; description: string; status: string; targetDate: string | null; completedAt: string | null; version: number; tasks?: { id: string; identifier: string; title: string }[]; updatedAt: string };
export type Artifact = { id: string; projectId: string | null; flowId: string | null; taskId: string | null; type: string; origin: string; canonicality: string; verification: string; title: string; summary: string; currentRevisionId: string | null; version: number; updatedAt: string; repositoryReference?: { repositoryFullName: string; path: string; commitSha: string | null; contentHash: string | null; verifiedAt: string | null; verificationNote: string } | null; revisions?: { id: string; revision: number; nativeContent: string; contentHash: string; createdAt: string }[]; relations?: { id: string; fromArtifactId: string; toArtifactId: string; relation: string }[] };
